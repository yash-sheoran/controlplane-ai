# ILLUSTRATIVE PLACEHOLDER — see CustomerSupport.yaml's header note. The
# document describes InternalKnowledge only as "semi-real-time,
# employee-facing, moderate risk tolerance", with no concrete numbers
# anywhere, including no latency figure (Section 3 Step 4 names one only
# for CustomerSupport and DecisionSupport). These values are NOT sourced
# from the document.
use_case_id: InternalKnowledge
geography: []
thresholds:
  block:
    data_leakage_risk: 8
    safety_risk: 9
    toxicity_risk: 9
  human_review:
    safety_risk: 7
    policy_violation_risk: 8
    hallucination_risk: 8
  modify:
    data_leakage_risk: 5
    pii_detected: true
  verify:
    hallucination_risk: 6
    correctness: 5
max_retries: 2
latency_budget_ms: 10000
require_human_review_for_final_block: false
# session_risk_window's default of 5 is doc-grounded (Section 6.1:
# "default N = 5"); session_risk_threshold is an illustrative placeholder
# (see CustomerSupport.yaml's header note).
session_risk_window: 5
session_risk_threshold: 5.5
